import { useState } from "react";
import {
  Alert,
  Button,
  ScrollView,
  StatusBar,
  Text,
  TextInput,
  View,
} from "react-native";

export default function App() {
  return (
    <ScrollView className="mt-${StatusBar.currentHeight} p-10">
      <View className="h-52 w-64 border border-gray-400 rounded-lg">
        <Text className="text-lg font-bold text-center text-white bg-sky-400 rounded-t-lg">
          Am Carding
        </Text>
        <Text className="text-base p-3">
          This makes them a performant choice for displaying long lists of
          data.This makes them a performant choice for displaying long lists of
          data.
        </Text>
      </View>
      <View className="mt-4 h-52 w-64 border border-gray-400 rounded-lg">
        <Text className="text-lg font-bold text-center text-white bg-sky-400 rounded-t-lg">
          Am Carding
        </Text>
        <Text className="text-base p-3">
          This makes them a performant choice for displaying long lists of
          data.This makes them a performant choice for displaying long lists of
          data.
        </Text>
      </View>
      <View className="mt-4 h-52 w-64 border border-gray-400 rounded-lg">
        <Text className="text-lg font-bold text-center text-white bg-sky-400 rounded-t-lg">
          Am Carding
        </Text>
        <Text className="text-base p-3">
          This makes them a performant choice for displaying long lists of
          data.This makes them a performant choice for displaying long lists of
          data.
        </Text>
      </View>
      <View className="mt-4 h-52 w-64 border border-gray-400 rounded-lg">
        <Text className="text-lg font-bold text-center text-white bg-sky-400 rounded-t-lg">
          Am Carding
        </Text>
        <Text className="text-base p-3">
          This makes them a performant choice for displaying long lists of
          data.This makes them a performant choice for displaying long lists of
          data.
        </Text>
      </View>
      <View className="mt-4 h-52 w-64 border border-gray-400 rounded-lg">
        <Text className="text-lg font-bold text-center text-white bg-sky-400 rounded-t-lg">
          Am Carding
        </Text>
        <Text className="text-base p-3">
          This makes them a performant choice for displaying long lists of
          data.This makes them a performant choice for displaying long lists of
          data.
        </Text>
      </View>
    </ScrollView>
  );
}
